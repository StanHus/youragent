# youragent

**This package is now published as [`agentize`](https://www.npmjs.com/package/agentize).**

`youragent` is kept as a legacy alias — same code, same scaffold, same everything. It depends on `agentize` and forwards to it.

## Use

```bash
# New canonical name (preferred):
npx agentize

# Legacy (still works, forwards to agentize):
npx youragent
```

See the canonical package for full documentation: https://www.npmjs.com/package/agentize
